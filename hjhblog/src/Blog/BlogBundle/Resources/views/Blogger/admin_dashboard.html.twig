{% extends '::base.html.twig'%}

{% block body %}
    <div class="row">
        {# <form action="{{path('BlogBundle_blog_user_dashOperations')}}" method="get"> #}
        <div class="panel panel-danger">
            <div class="panel-heading">
                <div class="row">
                    <h3 class="panel-title text-center">DASHBOARD BLOGGER</h3>
                    
                </div>
            </div>
            <div class="panel-body">
            <div class="row">
                <div class="col-lg-12">
                    <table class="table" id="table">
                        <thead>
                            <tr>
                                <th><input type="checkbox" id="main_checkbox" ></th>
                                <th>ID</th>
                                <th>SURNAME</th>
                                <th>FIRSTNAME</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for blogger in bloggers %}
                                <tr>   
                                    <td><input type="checkbox" class="blogger_blogger" name="blogger_id[]" value="{{blogger.id}}"></td>
                                    <td>{{blogger.id}}</td>
                                    <td>{{blogger.surname}}</td>
                                    <td>{{blogger.firstname}}</td>
                                </tr>
                            {% else %}
                                <p>No Bloggers ...</p>
                            {% endfor %}

                        </tbody>
                    </table>
                    <hr>
                </div>
            </div>

            </div> 
            <div class="panel-footer clearfix">
                <div class="pull-right">
                    <input type="submit" id="edit_blogger" name="action" class="btn btn-primary hide" value="Edit"></input>
                    <input type="submit" id="remove_blogger" name="action" class="btn btn-danger hide" value="Remove"></input>
                </div>
            </div>
        </div>
    {# </form> #}
    </div>

    <div class="row">
        {# <form action="{{path('BlogBundle_blog_user_dashOperations')}}" method="get"> #}
        <div class="panel panel-info">
            <div class="panel-heading">
                <div class="row">
                    <h3 class="panel-title text-center">DASHBOARD CATEGORY</h3>
                    
                </div>
            </div>
            <div class="panel-body">
            <div class="row">
                <div class="col-lg-12">
                    <table class="table" id="table">
                        <thead>
                            <tr>
                                <th><input type="checkbox" id="main_checkbox" ></th>
                                <th>ID</th>
                                <th>NAME</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for category in categories %}
                                <tr>   
                                    <td><input type="checkbox" class="category_category" name="category_id[]" value="{{category.id}}"></td>
                                    <td>{{category.id}}</td>
                                    <td>{{category.name}}</td>
                                </tr>
                            {% else %}
                                <p>No Blogs ...</p>
                            {% endfor %}

                        </tbody>
                    </table>
                    <hr>
                </div>
            </div>

            </div> 
            <div class="panel-footer clearfix">
                <div class="pull-right">
                    <input type="submit" id="edit_category" name="action" class="btn btn-primary hide" value="Edit"></input>
                    <input type="submit" id="remove_category" name="action" class="btn btn-danger hide" value="Remove"></input>
                </div>
            </div>
        </div>
    {# </form> #}
    </div>


    <div class="row">
        {# <form action="{{path('BlogBundle_blog_user_dashOperations')}}" method="get"> #}
        <div class="panel panel-primary">
            <div class="panel-heading">
                <div class="row">
                    <h3 class="panel-title text-center">DASHBOARD BLOG</h3>
                    
                </div>
            </div>
            <div class="panel-body">
            <div class="row">
                <div class="col-lg-12">
                    <table class="table" id="table">
                        <thead>
                            <tr>
                                <th><input type="checkbox" id="main_checkbox" ></th>
                                <th>ID</th>
                                <th>BLOG TITTLE</th>
                                <th>CATEGORY</th>
                                <th>BLOGGER/AUTHOR</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for blog in blogs %}
                                <tr>   
                                    <td><input type="checkbox" class="blog_blog" name="blog_id[]" value="{{blog.id}}"></td>
                                    <td>{{blog.id}}</td>
                                    <td>{{blog.title}}</td>
                                    <td>{{blog.category.name}}</td>
                                    <td>{{blog.blogger.surname}} {{blog.blogger.firstname}}</td>
                                </tr>
                            {% else %}
                                <p>No Blogs ...</p>
                            {% endfor %}

                        </tbody>
                    </table>
                    <hr>
                </div>
            </div>

            </div> 
            <div class="panel-footer clearfix">
                <div class="pull-right">
                    <input type="submit" id="edit_blog" name="action" class="btn btn-primary hide" value="Edit"></input>
                    <input type="submit" id="remove_blog" name="action" class="btn btn-danger hide" value="Remove"></input>
                </div>
            </div>
        </div>
    {# </form> #}
    </div>

    <div class="row">
        {# <form action="{{path('BlogBundle_blog_user_dashOperations')}}" method="get"> #}
        <div class="panel panel-success">
            {# <div class="panel-heading">
                <div class="row">
                    <h3 class="panel-title text-center">DASHBOARD COMMENT</h3>
                    
                </div>
            </div> #}
           {#  <div class="panel-body">
            <div class="row">
                <div class="col-lg-12">
                    <table class="table" id="table">
                        <thead>
                            <tr>
                                <th><input type="checkbox" id="main_checkbox" ></th>
                                <th>COMMENT ID</th>
                                <th>BLOG ID</th>
                                <th>BLOGER ID</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for comment in comments %}
                                <tr>   
                                    <td><input type="checkbox" class="comment_comment" name="comment_id[]" value="{{comment.id}}"></td>
                                    <td>{{comment.id}}</td>
                                    <td>NULL</td>
                                    <td>
                                        {{comment.blogger}}}
                                    </td>
                                </tr>
                            {% else %}
                                <p>No Blogs ...</p>
                            {% endfor %}

                        </tbody>
                    </table>
                    <hr>
                </div>
            </div>

            </div>  #}
            <div class="panel-footer clearfix">
                <div class="pull-right">
                    <input type="submit" id="edit_dash" name="action" class="btn btn-primary hide" value="Edit"></input>
                    <input type="submit" id="remove_dash" name="action" class="btn btn-danger hide" value="Remove"></input>
                </div>
            </div>
        </div>
    {# </form> #}
    </div>

    
{% endblock %}