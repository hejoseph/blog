{% for category in categories %}
    <a  href="{{path('BlogBundle_blogger_category', {'category_id':category.id})}}" class="list-group-item category-blog"><span class="badge">{{ category.blogs|length}}</span>{{ category.name }}</a>
{% else %}
    No Category
{% endfor %}